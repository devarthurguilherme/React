const ExecuteFunction = ({handleShowMessage}) => {
    return(
        <div>
            <h2>Execute Function here!</h2>
            <button onClick={handleShowMessage}>ShowMessage here!</button>
        </div>
    );
};

export default ExecuteFunction;