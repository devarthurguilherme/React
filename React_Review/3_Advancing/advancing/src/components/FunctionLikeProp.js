const FunctionLikeProp = ({handlePrint}) => {
    return(
        <div>
            <h1>Funtion like a prop</h1>
            <button onClick={handlePrint}>Console here!</button>
        </div>
    );
};

export default FunctionLikeProp